#include <math.h>
#include <stdlib.h>
#include <stdio.h>
#include <stdbool.h>

int main(int argc, char  *argv[]) {
  int t =atoi(argv[2]);
  int p =atoi(argv[1]);

  int r =atoi(argv[3]);
  int f;
  int z;
  z=exp(r.t);
  f=p exp(z);
  printf("%d\n",f );
  return 0;
}
