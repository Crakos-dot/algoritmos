#include <math.h>
#include <stdlib.h>
#include <stdio.h>
#include <stdbool.h>
#include <time.h>


int main(int argc, char  *argv[]) {
  int n = atoi(argv[1]);
  int b;
  int i = 0;
  int suma;
while (i<=n) {
  printf("Dijite un numero" );
  scanf("%d",&b );
  if (b%2==1) {
    suma=suma+n;
  }
}

printf("%d\n",suma );









  return 0;
}
